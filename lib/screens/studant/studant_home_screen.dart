import 'package:flutter/material.dart';

class EstudanteHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Relatório Estudante")),
      body: Center(child: Text("Bem-vindo Estudante!")),
    );
  }
}
